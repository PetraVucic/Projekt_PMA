@model IEnumerable<PMA_Projekt_Brodovi.Models.Vlasnik>

@{
    ViewData["Title"] = "Index";
}

<h4>Registar Vlasnika</h4>
<hr/>


<table class="table table-hover table-dark">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImeVlasnika)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrezimeVlasnika)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OibVlasnika)
            </th>
            <th>
                <a asp-action="AddOrEdit" class="btn btn-outline-success"><i class="far fa-plus-square"></i> Novi</a>
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ImeVlasnika)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrezimeVlasnika)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OibVlasnika)
            </td>
            <td>
                <a asp-action="AddOrEdit" asp-route-id="@item.VlasnikId"><i class="fa fa-marker fa-lg"></i></a> 
                <a asp-action="Delete" asp-route-id="@item.VlasnikId" class="text-danger ml-1" onclick="return confirm('Jeste li sigurni da želite izbrisati ovaj podatak?'"><i class="fa fa-trash-alt fa-lg"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
